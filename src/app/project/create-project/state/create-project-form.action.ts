import {createAction} from "@ngrx/store";

export const increment = createAction('[CreateProject Component] Increment');
export const decrement = createAction('[CreateProject Component] Decrement');
export const reset = createAction('[CreateProject Component] Reset');
